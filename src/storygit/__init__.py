"""storygit — version control for story state.

The package is a pure Python library: a typed story state model (`domain`), a
content-addressed snapshot store (`store`), a deterministic dependency and
propagation graph (`graph`), and — added in later chunks — model providers,
proposal generation, continuity checking, and a preference layer.
"""

__version__ = "0.1.0"
